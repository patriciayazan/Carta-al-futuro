<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CartaAlFuturo">

        <!-- Pantalla 1: Bienvenida -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Pantalla 2: Escritura + Temporizador + Persistencia de Ciclo de Vida -->
        <activity
            android:name=".EscrituraActivity"
            android:exported="false"
            android:label="Escribir Carta"
            android:windowSoftInputMode="adjustResize" />

        <!-- Pantalla 3: Carta sellada (detalle de solo lectura) -->
        <activity
            android:name=".SelladoActivity"
            android:exported="false"
            android:label="Carta Sellada" />

        <!-- Pantalla 4: Mis cartas (lista de cartas de la sesión) -->
        <activity
            android:name=".MisCartasActivity"
            android:exported="false"
            android:label="Mis Cartas" />

        <activity
            android:name=".ListaCartasActivity"
            android:exported="false"
            android:label="Mis Cartas" />

    </application>

</manifest>
